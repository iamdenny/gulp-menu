{
  "name": "gulp-menu",
  "version": "0.2.0",
  "description": "A plugin for Gulp",
  "keywords": [
    "gulpplugin",
    "menu"
  ],
  "author": {
    "name": "Denny Lim",
    "email": "hi.iamdenny@gmail.com",
    "url": "https://github.com/iamdenny"
  },
  "repository": "iamdenny/gulp-menu",
  "scripts": {
    "test": "istanbul test _mocha --report html -- test/*.js --reporter spec",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "dependencies": {
    "inquirer": "^0.7.1"
  },
  "devDependencies": {
    "coveralls": "*",
    "event-stream": "*",
    "gulp": "*",
    "istanbul": "*",
    "mocha": "*",
    "mocha-lcov-reporter": "*",
    "should": "~2.1.0"
  },
  "engines": {
    "node": ">=0.8.0",
    "npm": ">=1.2.10"
  },
  "license": "MIT"
}
